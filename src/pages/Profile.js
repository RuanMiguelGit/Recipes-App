import React from 'react';
import Header from '../components/Header';

const Profile = () => (
  <div>
    <Header
      name="Perfil"
      foodClass="Profile"
      Show={ false }
    />
  </div>
);

export default Profile;
